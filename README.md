# cordacon

Step 1:

Use **gradle deployNodes** to create the nodes. 

Step 2:
Navigate inside **demo-server** and do a **gradle createServer**.now execute **demo-server/build/libs/runserver** to start the server

